import {category} from './category'
import {header, link, navigationItem, submenuItem} from './header'
import {feature, grid, text, cta, image, video} from './objects'
import {page} from './page'
import {product} from './product'

export const schemaTypes = [
  page,
  header,
  navigationItem,
  submenuItem,
  link,
  category,
  product,
  feature,
  grid,
  text,
  cta,
  image,
  video,
]
